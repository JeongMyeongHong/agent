# 실행 방법 요약

## 🎯 상황별 실행 가이드

### 1️⃣ 로컬 개발 (가장 간단) ⭐

**Docker 없이 바로 실행**

```bash
# 준비
uv sync

# 실행
./run.sh
```

📍 **포트**: 8000
📍 **DB**: 175.117.82.131:5432 (외부)
📍 **접속**: http://localhost:8000
📍 **문서**: [DEVELOPMENT.md](DEVELOPMENT.md)

---

### 2️⃣ Docker - Production 환경

**API + DB 모두 Docker로 실행**

```bash
./start-prod.sh
```

📍 **포트**: 38000 (API), 35432 (DB)
📍 **DB**: Docker 내부 PostgreSQL
📍 **접속**: http://localhost:38000
📍 **설정**: .env.prod

---

### 3️⃣ Docker - Local 환경

**API만 Docker, DB는 외부 사용**

```bash
./start-local.sh
```

📍 **포트**: 38000
📍 **DB**: 175.117.82.131:5432 (외부)
📍 **접속**: http://localhost:38000
📍 **설정**: .env.local

---

## 📊 비교표

| 방법 | 명령어 | 포트 | DB | Docker | 개발 |
|------|--------|------|-----|--------|------|
| **로컬** | `./run.sh` | 8000 | 외부 | ❌ | ⭐ |
| **Docker-Prod** | `./start-prod.sh` | 38000 | 내장 | ✅ | - |
| **Docker-Local** | `./start-local.sh` | 38000 | 외부 | ✅ | - |

---

## 🔧 환경 파일

```
.env          # 현재 사용 중 (run.sh가 자동 생성)
.env.local    # 로컬/Docker-Local용
.env.prod     # Production용
.env.example  # 템플릿
```

---

## 📝 테스트

### Swagger UI
http://localhost:8000/docs (로컬)
http://localhost:38000/docs (Docker)

### curl
```bash
# 로컬
curl -X POST http://localhost:8000/stock/analyze \
  -H "Content-Type: application/json" \
  -d '{"company": "TSLA"}'

# Docker
curl -X POST http://localhost:38000/stock/analyze \
  -H "Content-Type: application/json" \
  -d '{"company": "테슬라"}'
```

---

## 🛑 중지

```bash
# 로컬: Ctrl+C

# Docker-Prod
docker-compose down

# Docker-Local
docker-compose -f docker-compose.local.yml down
```

---

## 💡 추천

### 개발 중
→ `./run.sh` (Docker 없이)

### 배포 전 테스트
→ `./start-prod.sh`

### 외부 DB 테스트
→ `./start-local.sh`

---

## 🚨 문제 해결

### 포트 충돌
```bash
# 8000번 포트
lsof -i :8000
kill -9 <PID>

# 38000번 포트
lsof -i :38000
```

### DB 연결 실패
```bash
# 외부 DB 테스트
telnet 175.117.82.131 5432

# 또는
psql -h 175.117.82.131 -p 5432 -U postgres -d stock_analysis
```

### 의존성 문제
```bash
# 재설치
rm -rf .venv
uv sync
```

---

## 📚 전체 문서

1. **[QUICKSTART.md](QUICKSTART.md)** - 1분 빠른 시작
2. **[DEVELOPMENT.md](DEVELOPMENT.md)** - 로컬 개발 상세
3. **[DOCKER.md](DOCKER.md)** - Docker 전체 가이드
4. **[README.md](README.md)** - 프로젝트 개요

---

## ✅ 체크리스트

시작 전:
- [ ] Python 3.13 설치
- [ ] uv 설치 (`pip install uv`)
- [ ] .env.local 설정 (API 키)
- [ ] 의존성 설치 (`uv sync`)

Docker 사용 시:
- [ ] Docker Desktop 설치
- [ ] .env.prod 또는 .env.local 설정

---

## 🎯 한 줄 요약

```bash
# 가장 빠른 실행 (로컬 개발)
uv sync && ./run.sh
```
